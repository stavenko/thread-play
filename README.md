Some details on running this:

```
 cargo run;
```
uses code from `parallel::std`


```
 cargo run --features=with_tokio;
```
uses code from `parallel::tokio`

```
 cargo run --features=with_rayon;
```
uses code from `parallel::rayon`
